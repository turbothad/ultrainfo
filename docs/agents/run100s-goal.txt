/goal Process every non-terminal candidate in `db/events/onboarding/run100s-queue.yml` in source order. Follow the queue agent_contract and `docs/research/claude-code-goal-workflow.md`; run `bin/validate-run100s-queue` before selecting work and after each queue update. Work on one race at a time, sequentially on `main`. For each race: verify facts from current first-party sources; implement, test, and commit; run `/mattpocock-skills:code-review` against the iteration base SHA, then Claude Code `/code-review`; if review fixes change code, restart both reviews; push the final reviewed SHA, require green CI for it, release, deploy, and pass public health and race-page smoke checks before advancing. Record all required evidence in the queue. On three repeated identical blockers, record `blocked_needs_human` and continue only if the workflow permits. Finish only when no non-terminal candidates remain, `bin/validate-run100s-queue` passes, and the final status counts and evidence summary are reported in the transcript.
