<nav aria-label="On this race page" class="sticky top-14 z-30 border-y border-ink/10 bg-paper/95 backdrop-blur">
  <div class="mx-auto flex max-w-[1320px] overflow-x-auto px-5 sm:px-8">
    <% [
      [ "Race facts", "facts" ],
      [ "Course map", "course" ],
      [ "Aid & cutoffs", "aid-stations" ],
      [ "Crew & pacers", "crew" ],
      [ "Follow", "follow" ],
      [ "Sources", "sources" ]
    ].each do |label, anchor| %>
      <%= link_to label, "##{anchor}", class: "whitespace-nowrap border-r border-ink/10 px-4 py-3 font-mono text-[9px] font-medium uppercase tracking-[0.1em] text-stone no-underline transition first:pl-0 last:border-r-0 hover:text-ink focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-[-2px] focus-visible:outline-pine" %>
    <% end %>
  </div>
</nav>
