<nav aria-label="On this race page" class="sticky top-[65px] z-40 mt-10 border-y border-ink/10 bg-paper/90 backdrop-blur-md">
  <div class="mx-auto flex max-w-[1240px] gap-1 overflow-x-auto px-6 py-2 sm:px-10">
    <% [
      [ "facts", "Facts" ],
      [ "course", "Course" ],
      [ "aid-stations", "Aid stations" ],
      [ "crew", "Crew" ],
      [ "follow", "Follow" ],
      [ "sources", "Sources" ]
    ].each do |section_id, label| %>
      <%= link_to label, "##{section_id}", class: "shrink-0 rounded-md px-3 py-2 font-mono text-[10px] font-bold uppercase tracking-[0.1em] text-stone no-underline transition hover:bg-pine/10 hover:text-ink sm:text-[11px]" %>
    <% end %>
  </div>
</nav>
